# AGENTS.md

## 项目定位

本项目用于：

- 合规获取公开证券数据；
- 构建可复现的数据清洗、因子计算、策略生成与回测流程；
- 生成研究型投资策略报告；
- 不用于实盘自动交易、荐股、承诺收益或规避数据平台限制。

Codex 在本仓库中工作时，必须优先保证：合规性、可复现性、无未来函数、无数据泄露、可测试、可解释。

## 数据源合规规则

优先使用以下数据源：

1. BaoStock：A 股/指数/ETF 等历史行情与财务数据，适合本地研究和回测；
2. AKShare：仅调用公开接口，必须控制频率并缓存；
3. 交易所、基金公司、上市公司公告等公开披露文件；
4. 用户明确提供的本地 CSV、Excel、Parquet、SQLite、DuckDB 文件。

禁止：

- 绕过验证码、登录、签名、加密参数、App 私有接口、IP 封禁；
- 抓取付费数据、Level-2、会员数据、非公开接口；
- 高频并发扫站、压测式抓取；
- 将第三方数据做成对外 API、网页、机器人或商业分发；
- 在代码或报告中声称策略保证盈利、稳赚、无风险。

默认频率限制：

- 历史日线/财务数据：优先本地缓存，增量更新；
- 实时或准实时数据：默认不做；确需做，也只允许低频研究用途；
- 所有下载函数必须支持 `sleep`、重试、断点续传和本地缓存。

## 金融研究边界

输出策略时必须写明：

- 仅供学习、研究、论文或回测验证；
- 不构成投资建议；
- 回测收益不代表未来表现；
- 必须考虑交易成本、滑点、停牌、涨跌停、调仓延迟和数据可得性。

Codex 不得：

- 生成鼓励直接实盘自动交易的默认脚本；
- 默认连接券商接口下单；
- 输出“买入某只 ETF/股票必赚”之类结论；
- 为了提高回测结果而引入未来函数或过拟合参数。

## 推荐项目结构

```text
quant_project/
├─ AGENTS.md
├─ README.md
├─ pyproject.toml 或 requirements.txt
├─ config/
│  ├─ universe.yaml              # 股票/ETF 池、时间区间、频率、交易成本
│  └─ data_sources.yaml          # 数据源配置，禁止写入账号密码
├─ data/
│  ├─ raw/                       # 原始缓存，不手动改
│  ├─ interim/                   # 中间清洗结果
│  └─ processed/                 # 回测可直接读取的数据
├─ notebooks/                    # 研究草稿，不作为生产逻辑唯一来源
├─ reports/                      # HTML/Markdown/PDF 报告
├─ src/
│  ├─ data/                      # 数据下载、缓存、清洗
│  ├─ features/                  # 因子与指标
│  ├─ strategies/                # 策略信号
│  ├─ backtest/                  # 回测引擎或封装
│  ├─ analysis/                  # 绩效归因、风险指标
│  └─ utils/                     # 日志、配置、日期工具
└─ tests/
   ├─ test_data_integrity.py
   ├─ test_no_lookahead.py
   ├─ test_backtest_accounting.py
   └─ test_strategy_signals.py
```

## 工程规范

- Python 版本：优先 3.10+。
- 数据存储：小项目可用 CSV/Parquet；多标的历史数据优先 Parquet 或 DuckDB。
- 所有核心函数必须有类型标注、docstring、异常处理和日志。
- 回测结果必须保存参数、数据版本、运行时间和 Git commit hash（若可用）。
- 不要把 API key、cookie、token、账号密码写入仓库。
- 若需要依赖包，先更新 `requirements.txt` 或 `pyproject.toml`，并解释用途。

## 回测硬性规则

Codex 在写回测代码时必须遵守：

1. 信号生成时间与交易执行时间分离：当天收盘信号，默认下一交易日执行；
2. 禁止用未来数据计算历史信号；
3. 每次调仓都计算交易成本；
4. 对 ETF/股票回测至少输出：累计收益、年化收益、年化波动率、最大回撤、夏普比率、胜率、换手率；
5. 基准必须明确，例如沪深 300、中证 500、中证全指、目标 ETF 自身或等权组合；
6. 参数搜索必须分训练期/验证期/测试期，禁止只展示最优结果；
7. 报告必须包含失败案例、风险解释和局限性。

## Codex 执行流程

处理“爬取数据 + 生成策略 + 回测”任务时，按以下顺序执行：

1. 明确研究对象：ETF/股票池、时间区间、频率、基准、成本、调仓周期；
2. 检查数据源合规性，不合规则改用 BaoStock、AKShare 或本地文件；
3. 搭建数据缓存和清洗流程；
4. 进行数据完整性检查；
5. 生成 1–3 个简单、可解释、低参数策略，不做黑箱过拟合；
6. 回测并输出指标；
7. 进行稳健性检查：不同时间段、不同成本、不同参数；
8. 生成报告；
9. 运行测试，说明通过/失败项；
10. 给出下一步改进建议。

## 完成定义

任务完成时必须提供：

- 运行命令；
- 关键输出文件路径；
- 数据源与合规说明；
- 回测指标表；
- 策略逻辑解释；
- 风险与局限；
- 测试结果。
